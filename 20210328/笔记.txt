1.HTTP和HTTPS的区别？
    HTTPS比HTTP更安全
    HTTP默认端口号80
    HTTPS默认端口号443
2.get和post的选择
如果参数不长，并且传递的信息不够敏感 使用get
如果参数长，并且传递的信息不够敏感 使用post（默认的接口请求类型）
3.请求头构成：
    请求行（方法get、post）
    请求头（请求的属性, 冒号分割的键值对;每组属性之间使用\n分隔）
    空行（用来标识请求头结束的）
    body正文（get的body是空的，post的body有内容，body存储的是参数，而get的参数已经在url中拼接。所以可以为空）
4.状态码
    200  请求成功
    301  所请求的页面已经转移到一个新的url，永久重定向
    302  所请求的页面临时转移到一个新的url，临时重定向
 （301和302的区别：1.  301是永久重定向，302是临时重定向  2.对于搜索引擎的收录权重是不同的，301 收录权重比302高）
    403  禁止访问所请求的页面
    404  服务器无法找到所请求的页面
    500  未完成的请求，服务器遇到了一个意外的状况
    502   未完成的请求，服务器从上游服务器收到无效响应
    504   网关超时
5.HTTP常见Header
    Content-Type: 数据类型
    (text/html：html页面)
  （text/css: css文件）
    (application/javascript: JS文件  后端接口最常用)
  （）
    Content-Length: Body的长度（用于初步的校验数据在传递过程中是否丢失）
    Host: 客户端告知服务器, 所请求的资源是在哪个主机的哪个端口上;
    User-Agent: 声明用户的操作系统和浏览器版本信息;
    referer: 当前页面是从哪个页面跳转过来的;
    location: 搭配3xx状态码使用, 告诉客户端接下来要去哪里访问;
    Cookie: 用于在客户端存储少量信息. 通常用于实现会话(session)的功能;
6.servlet
    作用：它是一个框架（帮助后端工程师开发Java api的框架）
7.maven
    servlet是依赖于maven构建的
    maven是一个项目管理工具
        --用来管理外部依赖（项目中使用的外部jar包）
        --可以使用maven打包项目
        --将项目进行部署
